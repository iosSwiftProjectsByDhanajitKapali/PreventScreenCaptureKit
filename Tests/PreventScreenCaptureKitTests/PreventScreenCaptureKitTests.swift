import XCTest
@testable import PreventScreenCaptureKit

final class PreventScreenCaptureKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PreventScreenCaptureKit().text, "Hello, World!")
    }
}
